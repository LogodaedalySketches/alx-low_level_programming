#include "main.h"
/**
 * _islower - lowercase characters
 * @c: 'check for lower case c'
 * Return: 1 for lowercase c and 0 for everything else
 */
int _islower(int c)
{
	if (c >= 97 && c <= 122)
	{

	return (1);
	}
	return (0);
}
