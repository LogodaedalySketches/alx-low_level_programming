#include <stdio.h>
/**
 * main - Entry point
 * Description: 'numbers of base16'
 * Return: always 0
 */
int main(void)
{
	int n;
	int o;

	for (n = 48; n <= 57; n++)
	{
		putchar(n);
	}
	for (o = 97; o <= 102; n++)
	{
		putchar(o);
	}
	putchar('\n');
	return (0);
}
